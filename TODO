TODO:

--Bigger nodes are still bouncing based on their tiny size

--Dijkstra's on the nodes

-frame rate display




Code graveyard:

        /*
        canvas.addEventFilter(MouseEvent.MOUSE_PRESSED, (event) ->
                    mouseClicks.add(new double[] {event.getX(), event.getY()})
        );
        */



System.out.println(pos[0] + " " + pos[1]);