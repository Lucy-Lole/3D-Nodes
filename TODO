TODO:

everything :)


---Node boundries

--Node creating on click

--deleting nodes

-Node connections??

-frame rate display




Code graveyard:

        /*
        canvas.addEventFilter(MouseEvent.MOUSE_PRESSED, (event) ->
                    mouseClicks.add(new double[] {event.getX(), event.getY()})
        );
        */
